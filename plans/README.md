# Implementation Plans

Generated by the improve skill on 2026-07-13 against commit `c689f8f`. Execute
in the order below unless dependencies say otherwise. Each executor: read the
plan fully before starting, honor its STOP conditions, and update your row
when done.

Repo quick reference (verification gates used by every plan):

- Install: `pnpm install`
- Typecheck: `pnpm typecheck`
- Lint: `pnpm lint` (oxlint), `pnpm lint:css` (stylelint)
- Unit tests: `pnpm test:unit` (Vitest; builds first via `pretest:unit`)
- E2E: `pnpm test:e2e` (Playwright, dev server on port 5174)
- Format: `pnpm format` / `pnpm format:check` (oxfmt)
- Branch convention: `advisor/NNN-<slug>`; conventional commit messages
  (`fix:`, `perf:`, `test:`, `docs:`, `chore:`, `refactor:`, `ci:`)

## Execution order & status

| Plan | Title                                                             | Priority | Effort | Depends on                       | Status           |
| ---- | ----------------------------------------------------------------- | -------- | ------ | -------------------------------- | ---------------- |
| 001  | Harden URL validation across export paths                         | P1       | M      | —                                | DONE (`366a46a`) |
| 002  | Escape video and email renderer interpolation                     | P1       | M      | 001                              | DONE (`9b0ea11`) |
| 003  | Sanitize callout attributes + visibility segment                  | P1       | S      | 001                              | DONE (`b1088b5`) |
| 004  | Validate header card style values                                 | P1       | S      | —                                | DONE (`2fa89e2`) |
| 005  | Fix async races (link search, header color) + gallery selector    | P1       | M      | —                                | DONE (`e9c1bc3`) |
| 006  | Gate per-keystroke DOM scans (drag-reorder, at-link)              | P1       | S      | —                                | DONE (`7227ab5`) |
| 007  | Drop semver runtime dep + debounce HtmlOutputPlugin               | P2       | S      | —                                | DONE (`eee1a97`) |
| 008  | Externalize yjs/y-websocket as optional peers                     | P2       | M      | —                                | DONE (`74eece1`) |
| 009  | Replace lodash with in-repo utilities                             | P2       | M      | —                                | DONE (`7549513`) |
| 010  | Consolidate duplicated plain-text paste handling                  | P2       | S      | —                                | DONE (`16ff3a3`) |
| 011  | Isolate private Lexical API usage                                 | P2       | M      | —                                | TODO             |
| 012  | Split registerKeyboardNavigation god function                     | P3       | L      | 018                              | TODO             |
| 013  | Remove dead/redundant dependencies                                | P2       | S      | —                                | TODO             |
| 014  | Replace `should` with Vitest `expect`                             | P2       | M      | —                                | TODO             |
| 015  | Wire coverage into verification + CI                              | P1       | M      | —                                | TODO             |
| 016  | Add composer component unit tests                                 | P2       | M      | —                                | TODO             |
| 017  | Add hook + markdown plugin unit tests                             | P2       | M      | — (005 for useSearchLinks cases) | TODO             |
| 018  | Stabilize e2e waits + triage skipped tests                        | P2       | M      | —                                | TODO             |
| 019  | DX/packaging hygiene (format CI, timeout, CLAUDE.md, React peers) | P2       | S      | —                                | TODO             |
| 020  | Add markdown card transformer                                     | P2       | S      | —                                | TODO             |
| 021  | Fix stale docs + public API overview                              | P2       | S      | 020                              | TODO             |
| 022  | Spike: headless HTML/email export API                             | P3       | M      | —                                | TODO             |
| 023  | Triage TODO cluster + draggable duplication decision              | P3       | M      | —                                | TODO             |

Status values: TODO | IN PROGRESS | DONE | BLOCKED (with one-line reason) |
REJECTED (with one-line rationale — finding fixed independently or approach
abandoned)

## Dependency notes

- **001 → 002, 003**: the escaping plans reference the validated-URL locals
  (`safeButtonUrl`) and helpers (`isSafeUrl`) that 001 establishes. 002 and
  003 are independent of each other.
- **018 → 012**: the keyboard-navigation split is a move-only refactor whose
  only meaningful safety net is the e2e suite; do not merge 012 against a
  flaky suite.
- **020 → 021**: the markdown-api doc rewrite must describe the shipped card
  set including the markdown card.
- **005 ↔ 017**: 017's `useSearchLinks` test file either extends 005's (if
  landed) or marks the known-race cases per its Step 2. Either order works;
  do not write conflicting versions of the same test file.
- **005/009 touch `useSearchLinks.ts`**: 005 changes behavior, 009 changes the
  debounce import. If both are in flight, merge 005 first.
- **015 ↔ 016/017**: 015 ratchets thresholds to the baseline; 016/017 raise
  measured coverage — re-ratchet upward when they land (015's maintenance
  policy).

Recommended first wave (all independent): 001, 004, 005, 006, 013, 015, 019.
Second wave: 002, 003 (after 001), 007, 009, 010, 011, 016, 017, 020, 023.
Final wave: 008 (packaging change — minor-version release note), 012, 014,
018, 021, 022.

## Findings considered and rejected

- **`lodash@4.18.1` pin is suspicious**: rejected — 4.18.1 is a real current
  registry release; the finding is the dependency's presence (plan 009), not
  the version.
- **`src/unsplash/` dead code**: rejected — directory already deleted in
  commit `5a550cb`.
- **`prettier` alongside `oxfmt`**: rejected — used only for deterministic
  HTML normalization in `test/utils/e2e.ts`; legitimate.
- **`react-highlight` in demo**: originally flagged low-value; included in
  plan 013 since removal is trivial and the user asked for all findings.
- **Opt-in node/transformer list for the markdown API** (docs open
  question 1): not selected by the user for planning; noted in plan 021/022
  as open.
- **Cross-fetch audit signal**: `pnpm audit --prod` reported no known
  vulnerabilities during the audit.

## What the audit did not cover

- `src/plugins/behaviour/*` drag-drop handler internals beyond the flagged
  hot path.
- Deep React render-memoization audit per card component.
- E2E test content correctness (assertions quality), demo app internals,
  Storybook stories.
- Minified per-module bundle breakdown (byte savings in plans 007–009 are
  sourcemap-based estimates).
- The `inkling-card-gallery` sibling repo (plan 023 investigates).

# Implementation Plans

Generated by the improve skill on 2026-07-13. Execute in the order below unless dependencies say otherwise. Each executor: read the plan fully before starting, honor its STOP conditions, and update your row when done.

Planned against commit `c295b9c`.

## Execution order & status

| Plan | Title                                                                 | Priority | Effort | Depends on | Status      |
| ---- | --------------------------------------------------------------------- | -------- | ------ | ---------- | ----------- |
| 001  | Fix capture-phase listener leak in `usePinturaEditor`                 | P1       | S      | —          | DONE        |
| 002  | Remove ReDoS-prone regexes from `sanitizeHtml`                        | P1       | S      | —          | DONE        |
| 003  | Sanitize caption HTML in image, gallery, and code-block renderers     | P1       | S      | —          | DONE        |
| 004  | Validate navigation URLs in gallery and file card renderers           | P1       | S      | —          | DONE        |
| 005  | Validate iframe `data-src` in `set-src-background-from-parent` helper | P1       | S      | —          | DONE        |
| 006  | Replace barrel imports in card wrapper nodes                          | P2       | S      | —          | DONE        |
| 007  | Remove dead dependencies and leftover Unsplash fixtures               | P2       | S      | —          | DONE        |
| 008  | Fix stale README instructions                                         | P2       | S      | —          | DONE        |
| 009  | Create `AGENTS.md` for AI onboarding                                  | P2       | S      | —          | DONE        |
| 010  | Add GitHub Actions CI workflow                                        | P1       | M      | —          | DONE        |
| 011  | Fix `ImageNodeComponent` file-dialog effect dependency array          | P2       | S      | —          | DONE        |
| 012  | Scope drag/drop hover suppression to the editor root                  | P2       | M      | —          | DONE        |
| 013  | Add unit tests for keyboard navigation plugin                         | P1       | L      | —          | DONE        |
| 014  | Add unit tests for slash and plus card menus                          | P1       | L      | —          | DONE        |
| 015  | Implement decorator-card markdown transformers                        | P1       | L      | —          | DONE        |

Status values: `TODO` | `IN PROGRESS` | `DONE` | `BLOCKED` (with one-line reason) | `REJECTED` (with one-line rationale).

## Dependency notes

- Plan 009 (`AGENTS.md`) is cheap and should land early so later executors have a local reference for commands and conventions.
- Plan 010 (CI workflow) can land anytime after the unit-test baseline is green; it currently is green at `c295b9c`.
- Security plans 001–005 are independent and can parallelize.
- Plan 006 (barrel imports) is independent but touches the same files as future card work; do it before adding new cards.
- Plans 013–014 (keyboard/menu tests) and Plan 015 (markdown transformers) are independent but each multi-day.

## Findings considered and rejected

- `PERF-02` / `PERF-03` / `PERF-04` / `PERF-05` / `PERF-07` / `PERF-08`: folded into broader perf work or lower leverage than the selected perf plans; not written up separately.
- `SECURITY-04` (unvalidated media URLs in bookmark/audio frontend renderers): closely related to `SECURITY-03`; the bookmark renderer already sanitizes captions and the media-src policy is best handled as a follow-up after `SECURITY-003` lands.
- `TECH-02`, `TECH-03`, `TECH-05`, `TECH-06`: valid debt but lower leverage than the barrel-import and dead-deps plans; can be re-audited after 006/007 land.
- `TEST-03`–`TEST-07`: specific critical-path coverage gaps that are partially addressed by Plans 013–014; remaining gaps can be re-audited after these land.
- `DIRECTION-02`–`DIRECTION-08`: direction items from the subagent audit; presented to the user as options but only `DIRECTION-01` (card markdown transformers) was selected for a plan in this cycle.
